import React from 'react';
import styled from 'styled-components';

const Header = () => <Wrapper>myTunez</Wrapper>;

const Wrapper = styled.div`
  color: blue;
  margin: 15px;
  font-size: 25px;
  font-family: Verdana, Helvetica, sans-serif;
`;

export default Header;
